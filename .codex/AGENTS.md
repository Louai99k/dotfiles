# Global Codex Instructions

Do not run tests, typecheck, lint, build, start dev server, Playwright, simulator, emulator, app launch, or any verification command unless I
explicitly ask.

After edits, only inspect changed files/diff if useful.

If repo/project instruction conflicts, repo/project instruction wins.
